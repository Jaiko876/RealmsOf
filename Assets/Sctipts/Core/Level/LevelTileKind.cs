namespace Game.Core.Level
{
    public enum LevelTileKind
    {
        Empty = 0,

        // Terrain
        GrassTop = 10,
        Dirt = 11,
        Stone = 12,

        // Decorations / special (на будущее)
        Platform = 20
    }
}
